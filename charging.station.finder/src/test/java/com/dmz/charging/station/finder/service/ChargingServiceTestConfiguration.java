package com.dmz.charging.station.finder.service;

import org.mapstruct.factory.Mappers;
import org.mockito.Mockito;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.Primary;
import org.springframework.context.annotation.Profile;

import com.dmz.charging.station.finder.service.mapper.StationMapper;


@Configuration
public class ChargingServiceTestConfiguration {

	/*
	 * @Bean
	 * 
	 * @Primary public FinderService service() { return
	 * Mockito.mock(ChargingStationFinderImpl.class); }
	 */
	 
	/*
	 * @Bean
	 * 
	 * @Primary public StationMapper service() { return
	 * Mockito.mock(StationMapper.class); }
	 */
	 
}
